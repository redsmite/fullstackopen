# New note in SPA

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: user writes note and clicks save

    Note right of browser: JS handles the event (no form submission)

    browser->>server: POST /exampleapp/new_note_spa (JSON data)
    activate server
    server-->>browser: success response (201 created)
    deactivate server

    Note right of browser: browser updates notes list without reloading the page